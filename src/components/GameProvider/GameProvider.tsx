import React from "react";
import styles from "./GameProvider.module.scss";

const GameProvider: React.FC = () => {
  return <div className={styles.gameSearch}></div>;
};

export default GameProvider;
